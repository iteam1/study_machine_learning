val a = 100
println(a)

// a = 200 // Error val can not be change value

// define datatype
val d:Int = 400
println(d)

// a val already define can not change datatype in script
// but in scala REPL
val c:Double = 400
println(c)

var b = 21.2
println(b)

b = 22.1 // variable can change the value
println(b)

var e = 400:Double
println(e)

val f= a + "," + b
println(f)
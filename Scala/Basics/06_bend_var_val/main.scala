val `my greatest gift in dollars` = 500
println(`my greatest gift in dollars`) // keyword import

val `magic` =  19
println(magic)

/*
OPCHAR
unicode character from \u0020-\u007F
*/

val isThisMySalaryPerYear_? = 5000

val thisShouldBeMyRaise_? = isThisMySalaryPerYear_? + 50000

val `void` = 100

val `false` = true

val `return` = 90

val res = if (`false`) `void` else `return`
println(res)